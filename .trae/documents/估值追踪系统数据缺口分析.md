# 估值追踪系统 — 数据缺口分析

> 日期：2026-08-15（本次更新：hithink-finance skill 已升级并验证，缺口 #1/#2 已实现、#3/#4/#6 部分可解）｜ 范围：当前 valuation-tracker 看板（Tags 侧边栏 / 二级公司列表 / 单公司看板 / 多公司横向比较 / Tag 篮子看板）

## 1. 当前已支持的数据（无缺口）

| 数据 | 来源 | 备注 |
|------|------|------|
| 实时价 / 涨跌幅 / 总市值 | 东财 push2 `ulist.np/get` | 同花顺无市值字段，已实测可用 |
| PE-TTM / PB(MRQ) / PS-TTM / PCF-TTM | 同花顺 `/valuations/snapshot` | 三源并发，失败降级部分字段 |
| 近 N 日 K 线（前复权 OHLCV） | 同花顺 `/kline`（interval=1d） | 60s 缓存 |
| 六维评分 + 系统加权综合分 | 笔记 frontmatter `scores` + `composite.ts` | 权重一处可改 |
| 目标市值（悲观/合理/乐观）+ 安全边际分档 | 笔记 frontmatter `target_market_cap_yi` | 当前市值 vs 目标折算偏离度 |
| Forward PE（值/基准净利/影响因素/发展方向） | 笔记 frontmatter `forward_pe` | backfill 生成 |
| 质量筛查结论（GREEN/YELLOW/RED + 质量分） | 笔记正文正则提取 | 来自 quality-screen.ts 段落 |
| 基本面更新状态（调研截止后有无新定期报告） | 巨潮 cninfo 公告查询 | 6h 缓存，可 `?refresh=1` |
| **财务三表结构化字段（`financials` 块）** | **同花顺三表多期序列现算** | **backfill 生成：quality-screen 8 项 + 近 5 年年报趋势（history）** |
| 笔记全文与关键章节（估值区间/跟踪指标/财报精读） | 笔记 Markdown | 轻量渲染 |
| 标签归类（行业/细分/风险）+ 平均涨跌 | 笔记 tags + 行情 | Tag 侧边栏聚合 |

## 2. 看板需求 vs 数据缺口

| # | 看板想展示 | 当前状态 | 缺口数据 | 建议来源 |
|---|-----------|---------|---------|---------|
| 1 | **财务三表结构化指标**（ROE/毛利率/净利率/OCF-NI/负债率/营收增长/净利增长/PE，即 quality-screen 8 项） | ✅ **已实现**：backfill 写入 frontmatter `financials` 块（`report_period` + 8 项指标 + 5 年 history），后端 `research.ts` 已解析 | — | 同花顺 `financials/income-statements` + `balance-sheets` + `cash-flow-statements`（annual×5，现算指标） |
| 2 | **多期业绩趋势**（3-5 年营收/净利/ROE 序列 → 营收净利趋势图） | ✅ **数据源已就绪**：`financials.history` 已含近 5 年年报序列（营收/净利/ROE/净利率/OCF/负债率） | 仅差前端趋势图 | 同花顺三表多期序列（`limit=5`，backfill 已落库） |
| 3 | **精确每股目标价 / EPS** | ⚠️ **部分解决**：EPS 直接取利润表 `basic_eps`；总股本仍用 `市值÷现价` 反推（近似） | 总股本 / 流通股本 | 同花顺暂无股本端点；EPS 已可取 |
| 4 | **股息率 / 分红历史** | 🔧 **数据源已找到，待实现**：同花顺 `corporate-actions/adjustment-factors` 返回 `dividend_per_share` + `ex_date_ms`（除息日） | 股息率 = 每股分红 ÷ 现价，可现算 | 同花顺 `corporate-actions/adjustment-factors` |
| 5 | **行业 / 同业估值基准**（行业平均 PE、PB 分位） | 无行业聚合基准（同花顺财务指标端点明确不返回行业均值） | 按 tag 聚合的估值分布（可用现有数据自算）+ 行业指数估值 | 现有数据自算 + 东财行业估值 |
| 6 | **估值历史分位**（当前 PE 处于 3-5 年历史百分位） | 🔧 **可推算**：历史 EPS（利润表 `basic_eps` 序列）÷ K 线收盘价 → 历史 PE 序列 | 历史 PE 序列 | 三表历史 EPS + K 线自算（近似） |
| 7 | **公告原文 PDF 直达**（基本面需更新时跳转原文） | 巨潮接口已返回 `adjunctUrl`，后端未透出 | 后端 `/api/fundamentals` 增加 `pdfUrl` 字段 | cninfo 返回体已含，改后端一行透传 |
| 8 | **比较看板财务维度**（营收/净利/ROE 横向比较行） | ✅ **数据源已就绪**：`financials` 块已含营收/净利/ROE/净利率等 | 同 #1（差前端比较行渲染） | 同 #1 |
| 9 | **资金面**（北向/主力资金流） | 无（同花顺仅龙虎榜，无北向/主力资金流） | 资金流接口 | 东财资金流 API（可选，非投资框架必需） |

## 3. 优先级建议

**P0（看板核心闭环）**
- #1 `financials` 结构化：✅ **已完成**（backfill.ts 增加 `financials` 块 + 后端 research.ts 解析），下一步为单公司看板财务卡与比较表财务行渲染
- #7 公告 PDF 链接透出：后端 fundamentals 路由补 `pdfUrl`，需更新时一键跳转原文

**P1（趋势与基准，中期）**
- #2 多期财务序列：✅ **数据已落库**（`financials.history` 近 5 年），仅差营收/净利/ROE 趋势图
- #4 股息率/分红历史：数据源已确认（复权因子事件流），可加 `dividend_yi` 字段 + 股息率展示
- #5 行业估值聚合：由 Tag 侧边栏数据前端自算行业 PE 均值/中位数即可（零成本）
- #6 估值历史分位：用 `financials.history` 的 EPS 序列 × K 线收盘价推算历史 PE 序列，看板显示"当前 PE 历史分位"

**P2（增强，按需）**
- #3 精确股本（当前反推足够监控）、#9 资金流

## 4. 影响评估

- ✅ 缺 #1/#8 已补齐：比较表可直接用 `financials` 块回答"谁的基本面更扎实"（营收/净利/ROE 横向对比）
- 缺 #2/#6 的趋势与历史分位仍待前端：数据已在 `financials.history`，安全边际判断的时间维度待渲染
- 其余缺口为增强项，不阻塞现有市值监控主链路
